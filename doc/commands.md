<!-- BEGIN init-project:managed -->
# 构建命令

> 来源：项目 manifest（package.json / pyproject.toml / Makefile / Maven / Gradle 等）静态扫描。
> Phase B 中 AI **必须**与用户确认并补全构建命令，每条命令追加 `# 说明` 注释。
> 不要把 src/ 下的源文件路径写成命令。

## 待补全（Phase A 未自动检测到构建命令）

请在 Phase B 中向用户询问并补全：

- 编译 / 构建命令
- 打包 / 产物输出命令
<!-- END init-project:managed -->
